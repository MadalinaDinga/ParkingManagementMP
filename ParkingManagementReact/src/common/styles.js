import {StyleSheet, Dimensions} from 'react-native';

//TODO: change all styles to be taken from this file
const {width, height} = Dimensions.get('window')
const styles = StyleSheet.create({

});

export default styles;